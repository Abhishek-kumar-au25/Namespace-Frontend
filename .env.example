# Example environment variables
# Copy this file to ".env" (or ".env.local") and fill in the values.

# Frontend (Create React App) - public values only (must start with REACT_APP_)
REACT_APP_BACKEND_URL=http://localhost:4000
REACT_APP_FRONTEND_URL=http://localhost:3000
REACT_APP_ENV=development

# ----------
# Backend (server-only) - DO NOT PUT THESE IN CLIENT-SIDE (no REACT_APP_ prefix)
# Place these in your server's .env file (or in your hosting provider's secret manager)
# ----------
# AI / LLM provider
OPENAI_API_KEY=
LLM_PROVIDER=openai
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_KEY=

# Email provider (SendGrid example)
EMAIL_PROVIDER=sendgrid
SENDGRID_API_KEY=
EMAIL_FROM=notifications@yourdomain.com
EMAIL_TO=kartikeya@yourdomain.com

# SMTP alternative
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# Storage (optional)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
S3_BUCKET=

# Anti-spam
RECAPTCHA_SECRET_KEY=

# Monitoring
SENTRY_DSN=

# Notes:
# - Do NOT commit your .env file with secrets to version control. Add it to .gitignore.
# - For frontend, only use variables that begin with REACT_APP_. Never expose secret keys to the browser.
# - The server should load server-only variables (OPENAI_API_KEY, SENDGRID_API_KEY, etc.) and keep them private.